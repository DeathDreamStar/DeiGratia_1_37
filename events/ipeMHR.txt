namespace = ipeMHR
normal_or_historical_nations = yes

# The Island of Socotra
country_event = {
	id = ipeMHR.1
	title = ipeMHR.1.t
	desc = ipeMHR.1.d
	picture = COLONIZATION_eventPicture

	fire_only_once = yes

	trigger = {
		religion_group = muslim
		OR = {
			tag = MHR
			AND = {
				owns = 402
				NOT = {
					exists = MHR
				}
			}
		}
		is_at_war = no
		1099 = {
			is_empty = yes
		}
		NOT = {
			current_age = age_of_discovery
		}
	}

	mean_time_to_happen = {
		months = 120
	}

	option = { # Socotra will be a valuable possession.
		name = ipeMHR.1.a
		1099 = {
			add_core = ROOT
			cede_province = ROOT
			change_trade_goods = fish
		}
	}
}