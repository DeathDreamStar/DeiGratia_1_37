country_decisions = {
	bulgaria_nation = {
		major = yes
		potential = {
			normal_or_historical_nations = yes
			was_never_end_game_tag_trigger = yes
			NOT = { has_country_flag = formed_bulgaria_flag }
			OR = {
				ai = no
				is_playing_custom_nation = no
			}
			NOT = { tag = BUL }
			OR = {
				ai = no
				AND = {
					NOT = { exists = BUL }
					num_of_cities = 3 #Carnage
				}
			}
			NOT = { tag = HLR }
			NOT = { tag = PAP }
			primary_culture = bulgarian
			is_colonial_nation = no
			OR = {
				is_former_colonial_nation = no
				AND = {
					is_former_colonial_nation = yes
					ai = no
				}
			}
		}
		provinces_to_highlight = {
			OR = {
				province_id = 150   # Nikopol
				province_id = 1765  # Sofia
				province_id = 2746  # Vidin
				province_id = 2750	# Plovdiv
				province_id = 3834	# Varna
				province_id = 4704  # Tarnovo
			}
			OR = {
				NOT = { owned_by = ROOT }
				NOT = { is_core = ROOT }
			}
		}
		allow = {
			NOT = { exists = BUL }
			is_at_war = no
			is_free_or_tributary_trigger = yes
			is_nomad = no
			owns_core_province = 150	# Nikopol
			owns_core_province = 1765	# Sofia
			owns_core_province = 2746	# Vidin
			owns_core_province = 2750   # Plovdiv
			owns_core_province = 3834   # Varna
			owns_core_province = 4704   # Tarnovo
		}
		effect = {
			change_tag = BUL
			swap_non_generic_missions = yes
			remove_non_electors_emperors_from_empire_effect = yes
			if = {
				limit = { NOT = { government_rank = 3 } }
				set_government_rank = 3
			}
			random_owned_province = {
				ipe_add_base_tax_1 = yes
			}
			add_country_modifier = {
				name = "centralization_modifier"
				duration = 7300
			}
			add_prestige = 25
			if = {
				limit = { has_custom_ideas = no }
				country_event = { id = ideagroups.1 } #Swap Ideas
			}
			bulgaria_area = {
				limit = {
					NOT = { is_core = BUL }
					NOT = { is_permanent_claim = BUL }
				}
				add_permanent_claim = BUL
			}
			northern_bulgaria_area = {
				limit = {
					NOT = { is_core = BUL }
					NOT = { is_permanent_claim = BUL }
				}
				add_permanent_claim = BUL
			}
			thrace_area = {
				limit = {
					NOT = { is_core = BUL }
					NOT = { is_permanent_claim = BUL }
				}
				add_permanent_claim = BUL
			}
			vardar_area = {
				limit = {
					NOT = { is_core = BUL }
					NOT = { is_permanent_claim = BUL }
				}
				add_permanent_claim = BUL
			}
			macedonia_area = {
				limit = {
					NOT = { is_core = BUL }
					NOT = { is_permanent_claim = BUL }
				}
				add_permanent_claim = BUL
			}
			set_country_flag = formed_bulgaria_flag
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400 #
	}
	
}